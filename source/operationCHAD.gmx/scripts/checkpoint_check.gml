if (!instance_exists(par_actor))
    exit;

with(checkpoint_obj){

    var Yset=Yplace;

    if(CHK<global.CHECKPOINT)
    instance_destroy()
    
    
    var SETstate=MYstate;
    
    if(CHK=global.CHECKPOINT){
    
    
    JB_cam.x=x
    JB_cam.y=y
    
    obj_player.x=x+20
    obj_player.y=y+20
    
    
    with(JB_cam){
    
        if not(Yset=-1)
        Ycurrent=Yset
        state=SETstate
        }
    }
}
